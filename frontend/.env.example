# Must match the backend's ApiKey:Key (see backend/src/SmhiWeather.Api/appsettings.json).
# Copy to .env.local. Fine to expose here per the project's requirements - not a pattern for production.
VITE_API_KEY=local-dev-demo-key
